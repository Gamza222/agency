/**
 * Animation constants for AppBackground
 * These values match VideoBackground for consistency
 */

// Animation duration in milliseconds
export const ANIMATION_DURATION_MS = 2000; // 2 seconds

// Animation duration in seconds (for CSS)
export const ANIMATION_DURATION_S = ANIMATION_DURATION_MS / 1000;

// Animation completion offset (call onAnimationComplete slightly before animation ends)
export const ANIMATION_COMPLETE_OFFSET_MS = 800;
